<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Camera -->
    <uses-feature android:name="android.hardware.camera.any" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/appName"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SanPabLOOKEstablishment"
        tools:targetApi="31">
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".RegistrationProcessActivity"  />
        <activity android:name=".ConfirmRegistrationActivity" />
        <activity android:name=".ProfileSettingsActivity" />
        <activity android:name=".RegistrationCompleteActivity" />
        <activity android:name=".RegistrationCreateAccountActivity"/>
        <activity android:name=".EmailConfirmationSent" />
        <activity android:name=".SignInActivity" android:exported="true" />
        <activity android:name=".SignUpActivity" android:exported="true" />
        <activity android:name=".BottomNavBarActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>